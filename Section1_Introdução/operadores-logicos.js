/* 
    && AND (E)
    || OR (Ou)
    ! NOT (Negação)
*/

const comida = "pao"
const bebida = "agua"

if (comida == "pao" && bebida == "coca"){
    console.log("Pao e Coca")
} else {
    console.log("Pedido errado!")
}
